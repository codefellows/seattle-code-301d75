import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

// I deleted all of the test code, as well as reportWebVitals and the logo.svg
// I also removed reportWebVitals from here and from the imports at the top of this file
